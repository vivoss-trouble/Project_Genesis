[package]
name = "lazarus-scanner"
version = "0.1.0"
edition = "2024"

[dependencies]
lazarus-contracts = { path = "../lazarus-contracts" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
glob = "0.3"
toml = "0.8"
sha2 = "0.10"
