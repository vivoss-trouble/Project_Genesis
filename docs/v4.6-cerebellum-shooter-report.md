# v4.6 Cerebellum Shooter Live-Fire Report

## Test Envelope

- **Model:** `qwen2.5-coder-7b-instruct-q4_k_m.gguf`
- **Run:** `.genesis-state/live-fire/20260525T141555Z`
- **Duration:** 180 seconds
- **Arena:** Dynamic Arena at 60fps
- **Frame policy:** `FRESH_FRAME_TOLERANCE=2`
- **Commit under test:** `1b983b2`

## Telemetry Summary

```text
intents total              = 23
decoded actions            = 23
decoder failures           = 0
json_legal_rate_pct        = 100.00
fallback_actions           = 0
action_distribution        = noop=12, click_point=11
failure_distribution       = TargetOccluded=1
warning_distribution       = <none>
click_point frame_delta    = min 1, max 2
```

## Shot Ledger

Every `click_point` action in this run was resolved by the local Cerebellum Shooter. The projected action reason starts with:

```text
cerebellum shooter resolved:
```

The dynamic shots produced:

- `10` verified hits
- `1` physically valid failure: `TargetOccluded`
- `0` stale-frame failures
- `0` purifier fallbacks

## Boundary Proof: TargetOccluded

The single failed shot is the most important proof in the run.

The Cerebellum Shooter did not cheat. It converted the model's `aim_dynamic` intent into a fresh `click_point` with a valid frame delta. Arena then rejected the shot as `TargetOccluded`.

That means the architecture separated the two failure classes cleanly:

- **Old failure:** slow cognition produced stale coordinates.
- **Current boundary:** fast deterministic geometry can still lose to physical occlusion.

This is the desired result. The shooter owns only geometry. Arena owns world truth.

## Conclusion

v4.6 collapses the dynamic-click latency from the v4.5 range of roughly `170-200` frames to `1-2` frames without relaxing the physical judge and without adding strategy to Core.

The high-frequency UI time wall is broken, and the microkernel remains blank.
