[package]
name = "genesis-replay"
version = "0.1.0"
edition = "2024"

[dependencies]
genesis-contracts = { path = "../genesis-contracts" }
libloading = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
