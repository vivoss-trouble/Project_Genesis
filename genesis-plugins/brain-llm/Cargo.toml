[package]
name = "brain-llm"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
genesis-contracts = { path = "../../genesis-contracts" }
genesis-platform = { path = "../../genesis-platform" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
