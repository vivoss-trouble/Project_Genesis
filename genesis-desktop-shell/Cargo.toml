[package]
name = "genesis-desktop-shell"
version.workspace = true
edition.workspace = true
authors.workspace = true

[dependencies]
genesis-platform = { path = "../genesis-platform" }
genesis-sdk = { path = "../genesis-sdk" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
